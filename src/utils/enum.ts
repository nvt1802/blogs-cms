export enum ScreenBreakpoints {
  SM = 640,
  MD = 768,
  LG = 1024,
  XL = 1280,
  "2XL" = 1536,
}

export enum UserRole {
  ADMIN = "admin",
  AUTHOR = "author",
  USER = "user",
}

export enum ExpiresUnit {
  SECONDS = "s",
  MINUTES = "m",
  HOURS = "h",
  DAYS = "d",
  WEEKS = "w",
  YEARS = "y",
}
